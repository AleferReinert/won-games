import Container from 'components/Container/Container' import Heading from
'components/Heading/Heading' import * as S from './{{pascalCase name}}.styles'
type
{{pascalCase name}}TemplateProps = { children: React.ReactNode } const
{{pascalCase name}}Template = ({ children }:
{{pascalCase name}}TemplateProps) => { return (
<S.Wrapper>
  <Container>
    <Heading $line='left' $lineColor='secondary'>
      {{pascalCase name}}
    </Heading>
    {children}
  </Container>
</S.Wrapper>
) } export default
{{pascalCase name}}Template