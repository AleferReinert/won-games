import type { StoryObj, Meta } from '@storybook/react'
import { expect, within } from '@storybook/test'
import {{pascalCase name}}Page from './index'
import {{template}}Template from 'templates/{{template}}/{{template}}'

const meta: Meta<typeof {{pascalCase name}}Page> =  {
  title: 'Pages/{{pascalCase name}}',
  component: {{pascalCase name}}Page,
  decorators: [
    (Story) => (
      <{{template}}Template>
        <Story />
      </{{template}}Template>
    )
  ],
  parameters: {
    layout: 'fullscreen'
  },
} 

export default meta

type Story = StoryObj<typeof {{pascalCase name}}Page>

export const {{pascalCase name}}: Story = {
     play: async ({canvasElement, step}) => {
        const canvas = within(canvasElement)

				await step('',() =>{
	        expect()
				})
    }
}