import type { ReactElement } from 'react'
import type { NextPageWithLayout } from '../_app'
import Base from 'templates/Base/Base'
import * as S from './{{pascalCase name}}.styles'

type {{pascalCase name}}PageProps = {}

const {{pascalCase name}}Page = (props: {{pascalCase name}}PageProps & NextPageWithLayout) =>  {
  return (
    <>
      <S.Wrapper>
        {{name}}
      </S.Wrapper>
    </>
  )
}

{{pascalCase name}}Page.getLayout = function getLayout(page: ReactElement) {
  return <Base>{page}</Base>
}

export default {{pascalCase name}}Page
